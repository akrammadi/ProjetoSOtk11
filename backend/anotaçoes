// ROTA/RECURS0

//METODOS HTTP:
//GET:Buscar uma informação do back-end
//POST:Criar uma informação no back-end
//PUT: Alterar uma informação no back-end
//DELETE: Deletar uma informação no back-end
//usamos o INSOMNIA para rodar o post e os demais métodos

//Tipos de  parâmetros:
//Query Params: Parâmetros nomeados enviados na rota após '?' (Filtros, Paginação)
    //Ex: http://localhost:3333/users?name=putaria&idade=33
    // app.get('/users',(request,response) =>{
    //      const params = request.query
    //      console.log(params)  
                //retorna no console:{ name: 'putaria', idade: '33' }
//Route Params: Parâmetros utilizados para identificar recursos
    //Ex:http://localhost:3333/users/1
    // app.get('/users/:id',(request,response) =>{
    //     const params = request.params
    //     console.log(params)
//Request Body: Corpo da requisição, utilizado para criar ou alterar recursos
    // app.post('/users',(request,response) =>{
    //     const body = request.body
    //     console.log(body)
            //**Obs para colocar no início do código: app.use(express.json()) //capta o JSON do browser e trás como JS

//  BANCO DE DADOS
//SQL: MySQL, *SQLite*, PostgreSQL, Oracle, Microsoft SQL Server
//NoSQL: MongoDB, CouchDB, etc
//
//Driver:SELECT * FROM users (não usaremos)
//Query Builder: table('users').select('*').where()
